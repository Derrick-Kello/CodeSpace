/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Enable Turbopack for development
    // Check https://nextjs.org/docs/advanced-features/turbopack for more details
    // This will be enabled automatically in future Next.js versions
  },
}

module.exports = nextConfig
